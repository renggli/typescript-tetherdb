{
  "name": "@tetherdb/example-editor",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "clean": "node -e \"['dist', 'data'].forEach((d) => fs.rmSync(d, { recursive: true, force: true }))\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "tetherdb": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
